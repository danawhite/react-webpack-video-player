var webpack = require('webpack');
var HtmlWebpackPlugin = require('html-webpack-plugin');

const APP_PATH = __dirname + 'src/app';
const BUILD_PATH = __dirname + 'src/build'; 

module.exports = {
        
};